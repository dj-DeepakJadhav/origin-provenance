.venv/
__pycache__/
*.py[cod]
.env
.env.local
*.log
.pytest_cache/
.ruff_cache/
# All document storage is generated by `origin.cli ingest` and reproducible
# from the public source APIs. Committing it bloats the repo and mixes uuid-named
data/*
!data/ablation_suite.json
!data/hub_licence_projection.csv
!data/documents/
dist/
build/
.DS_Store

# Private ideation doc
IDEATION.md

# Lambda environment files carry DATABASE_URL, which contains the cluster
# password. The runbook keeps them in %USERPROFILE%; this is the backstop for
# when someone writes one into the repo instead.
*env*.json
response.json

# Video production & scratch build outputs
origin-video/
generate_voice*.py
list_voices.py
conv.json
titan.json
node_modules/
**/node_modules/



# Working notes: scoring analysis and task planning. Useful locally,
# not project documentation, and not something judges should read.
REMAINING.md

# Working notes: the 8.2 -> 9.5 roadmap. Local planning, not project docs.
ROADMAP_9.5.md
